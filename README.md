#React.js + Zurb Foundation with Webpack starter

##Installation

1. npm install
2. npm install bower -g
3. bower install

##Build or run
Use `webpack` to build or `webpack-dev-server` to start a development server